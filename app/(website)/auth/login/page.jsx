import Login from "@/components/website/login";

export const metadata = {
  title: "Login | Task Processing Hub",
  description: "Sign in to your account",
};

export default function LoginPage() {
  return <Login />;
}
